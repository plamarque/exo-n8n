.DS_Store

# Node (workflow SDK validation / codegen)
node_modules/

# Ephemeral SDK output and scratch (not committed)
work/

# Local French article (not committed; optional local copy for partners)
docs/ARTICLE-FR-partenaires-eXo.md

# Local env files
.env
*.env
!.env.example
!*.env.example

# Cursor IDE: ignore everything under .cursor except shared rules, project skills, and mcp example (no secrets)
.cursor/*
!.cursor/rules/
!.cursor/rules/**
!.cursor/skills/
!.cursor/skills/**
!.cursor/mcp.json.example
