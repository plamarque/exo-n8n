# TODO

- [ ] WF01 `WF01 - Email to Task (SDK)` (`zeVd0scWqU5vcOUq`): ajouter une vraie déduplication des emails pour éviter la recréation de tâches sur les mêmes messages à chaque run (clé recommandée: `email_id`, fallback `messageId` + `receivedDate`).
